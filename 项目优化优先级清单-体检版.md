# BossMate 项目优化优先级清单（代码体检版 · 6-20）

> 基于一轮代码扫描，非凭记忆。每项标注【实据 / 影响 / 工作量】。

## P0 — 商业化阻塞（你的动作，不通后面全白搭）

### 1. 阿里云短信接入
- 影响：不接，真客户收不到验证码、登不进来。
- 动作：你去阿里云申请签名/模板（审核 1-2 天）+ 装 SDK + 配 `.env`。代码已写好可插拔，SOP 已给。

### 2. ICP / 经营性备案 + 法务
- 影响：ToB 卖工具，平台备案是红线；真签约前要过。
- 动作：你侧办理。

## P0 — 数据完整性（我能动手，客户多了会咬人）

### 3. 外键无级联删除（系统性）
- **实据**：`schema.ts` 75 个 `references(...)`，仅 5 个带 `onDelete`。
- 影响：删租户/员工/内容时，关联表（contents/accounts/leads/messages/metrics…约 70 处）留**孤儿数据**——数据越积越脏，未来删租户=灾难。
- 工作量：中。需逐表判定 `cascade`（强归属，如租户→其内容）vs `set null`（弱引用），出一个 migration。**要谨慎，不能无脑 cascade**。

## P1 — 内容质量 / 智能（客户付费的核心理由，我能动手）

### 4. 数据闭环打通（选题飞轮目前空转）
- **实据**：`recordMetric` 写入器存在并被调用，但喂进去的不是真实平台数据；`dashboard.ts:204 totalReadsToday: 8500` 是**硬编码假数据**，微信/抖音 stats API 未接。`categoryFactors` 因无真实表现数据，权重全为 1。
- 影响：所谓"表现好的选题加权、越用越准"是摆设；效果分析页给客户看的也是假数。
- 工作量：中-大。需接平台真实阅读/互动回流（微信公众平台 stats / 抖音开放数据），或先让 Agent 抓回已发内容的真实数。

### 5. 期刊存量数据清洗（IF/分区出入）
- **实据**：生成端护栏已上（排除 ai_fabricated、复合IF 已露出），但库里存量脏数据未清。
- 影响：客户一眼能看出期刊数据对不对，直接影响信任。
- 工作量：中。跑数据核对 + 标记/修正脏值（可借 letpub 年度核对）。

## P1 — 合规 / 风控

### 6. 图片内容审核接入
- **实据**：`routes/video.ts:148 // 图片内容审核接口占位（一期不接真实审核）`。文本敏感词库（risk-control/dictionaries）已有，**图片侧没做**。
- 影响：发图文/视频封面若含违规图，平台会限/封号。
- 工作量：中。接阿里云/腾讯云内容安全（图片审核）API。

## P2 — 平台运营 / 扩展（客户变多后）

### 7. 平台监管层①
- 用量看板 / 冻结租户 / impersonate 排障（带审计）/ 计费。现在 SQL 手动顶。

### 8. 服务器迁移 + 升配（4核16G）
- 才能上 HTML→视频渲染、重度混剪等吃配置功能。

### 9. 便携包内置独立 Chromium（task #41）

## 技术债（103 处标记，非阻塞，顺手清）
- `dashboard.ts` 假数据 8500（接了真实数就删）
- 期刊官网截图 / 横幅图 TODO（video asset，非核心）
- 其余多为占位说明，可随相关功能迭代时清理。

---

## 建议执行顺序

1. **你并行去办** P0-1（短信）+ P0-2（备案）。
2. **我先做** P0-3（外键级联删除）——稳定性欠债，客户多了最危险，且现在改干净成本最低（数据还少）。
3. 然后 P1-4（数据闭环）或 P1-6（图片审核）二选一——前者提升"智能感"，后者堵合规风险，看你更怕哪个。
4. P1-5（数据清洗）可穿插。
5. P2 等真客户落地后再排。

**我的首推：P0-3 外键级联删除**（半天到一天，纯稳健性，零客户感知风险），做完再上 P1。你定。
