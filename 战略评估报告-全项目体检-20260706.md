# BossMate 全项目战略评估(2026-07-06)

> 评估方式:通读全部规划/运营文档 + 三路并行代码审查(AI 能力层 / 架构工程 / 产品商业面),关键论断逐条回源码核验。所有结论附证据位置,可直接复查。

---

## 一、一句话总评

**技术骨架的完整度远超商业验证的进度**——3.5 个月、795 次提交、约 11.5 万行代码,搭出了多租户、计费、合规、内容引擎、多平台分发、AI 客服、数字人的全套骨架,但真实付费客户只有 1 家(顺仕美途),定价没有数字,客服 FAQ 大量【待填】,开通流程上线不到一周未经真实客户验证。**当前最稀缺的不是功能,是第二个客户。** 接下来 30 天的所有决策都应该用一个问题过滤:"这件事能不能让第 2-5 家客户签下来、用起来、续下去?"

---

## 二、什么做对了(值得坚持的资产)

1. **期刊数据护城河是真的。** journal-enricher 有 7 个真实数据源(Crossref/OpenAlex/LetPub/万方/分区表等),crawler 20 个采集器,CSCD/北大核心目录落库脚本齐全。商业化方案里"丢了它你什么都不是"的判断正确,这确实是竞品抄不走的东西。

2. **质量治理理念在同类产品里领先。** 六维评分 80 分线(代码与标准文档逐字对齐)、AI 审稿员的设计(只裁灰区/红线留人/few-shot 校准/shadow-live 双模/一致率报表)、AI 客服的"宁转人工不瞎答"多层兜底、评分降级不伪装过线——这些是很多融资过亿的 AI 内容公司都没做到的克制和严谨。

3. **数据飞轮今天刚接上(0a36c25)。** 拔掉 8500 假数据、getarticlesummary 每日回流、选题权重真实化 [0.5,2.0]、标题修改捕捉——"越用越准"从摆设变成了真链路。这是本项目从"内容工具"变成"经营系统"的分水岭,但刚闭环、只覆盖微信、未经验证。

4. **单人 + AI 编码的开发范式本身是资产。** 红线文档、CC 落地清单、交接文档构成了"组织记忆",事故复盘(schema-drift-rescue、死测试清理)透明诚实。这套方法论本身可复制,是团队规模不变前提下吞吐量的保证。

---

## 三、薄弱点(按战略影响排序)

### 1. 商业面:骨架有了,血肉没填 【最高优先级,且多数不是代码问题】

短信未接(真客户收不到验证码登不进来)、ICP/经营性备案未办、定价只有套餐结构没有价格数字、客服 FAQ 收费/流程/案例全是【待填】、开通表单 7-03 才上线未经真实客户走通。**产品能力已具备,但合规和基础设施卡着真客户进不来。** 这几项大多需要老板亲自办理,AI 替代不了。

### 2. 三个"看起来有防护、实际没生效"的假保险 【工程可信度问题,修复成本低】

- **LLM 网关被主链路绕过**:model-router 有完整的熔断/按任务路由/主备竞速,但花钱最多的 article-skill(1546 行)和 video-skill 直接持有启动时注入的固定 provider(skills/index.ts:11),不走路由、不触发熔断、无重试超时包裹。DeepSeek 一抖,主链路裸奔。
- **CI 存在但带病运行**:约 47 个失败测试文件是"已知基线",红绿信号不可信;main 分支保护至今未开。
- **LLM 成本台账 0 写入**:cost-ledger 定义了 llm 类型但全项目从未写入一行;agents 里 tokensUsed 13 处硬编码 0。**老板看不到"这个月 AI 花了多少钱、哪个租户在烧钱"**,多租户 SaaS 的成本风险敞口以月账单为发现周期。

### 3. 质量无回归防线 【与核心卖点直接冲突】

198 个测试文件里 105 个(53%)是"读源码字符串+正则断言某句 prompt 还在",不能检测"prompt 没变但模型行为变了"。产品卖点是"内容质量 80 分",但没有任何黄金样本集(golden set)eval:阿里云侧升级 DeepSeek/Qwen 版本、或任何一次 prompt 调整,质量退化只能靠老韩读全文事后发现。**"老韩读全文"既是当前质量的保证,也是规模化的天花板。**

### 4. 从 1 到 10 个客户时会咬人的数据债

- Schema drift 未清零:生产库 23 张表 + 18 个列不在 schema.ts 里,rescue 文档承诺的 realignment 专项无落地痕迹。
- 外键 75 处 references 仅 5 处带 onDelete:删租户/员工/内容会留约 70 处孤儿数据。
- 校准样本塞在 contents.metadata JSONB 里("免 migration"),规模上来无法走索引。
- 单机 ECS + pm2 restart 直接重启,无冗余无灰度;已有 root 直接上服务器改代码致三向分叉的事故先例。

### 5. 跨行业扩张被锁死在最后一公里

每日自动生成写死走期刊库,选题池接不进管线——除学术同行外,其他行业客户无法真正自动化服务。商业化方案第二阶段(教育客户)目前是纸面。

### 6. 体验碎片化伤销售 demo

同一件事在 5-7 个地方各做一套,7 个页面做完没入口(frontend-interaction-audit 自查结论)。单人+AI 按 PR 堆功能的必然结果,对内能忍,对外销售演示是硬伤。

### 7. 杂项地雷

deploy-*.py 三个 base64 编码的部署脚本(258KB,硬编码已死服务器,文档明令禁跑但没删);worldmonitor/ 整个无关开源项目(66 项)混在仓库目录;243 个 git 分支无人清理;AI 客服无多轮记忆(kf-responder 全部调用不传 context,客户追问"那这个期刊审稿周期呢"会答非所问)。

---

## 四、接下来开发什么(建议路线)

### P0 · 本周-两周:签单阻断项清零(主题:销售就绪)

| 事项 | 性质 | 说明 |
|---|---|---|
| 短信接入 + 备案启动 | 老板动作 | 客户登录的物理前提;备案周期长,今天启动 |
| FAQ 内容填齐 + 客服验收题集实测 | 运营内容 | 技术已就绪,缺的是话术/报价/案例定稿 |
| 用真实流程开通 1 家新客户(哪怕免费试用) | 验证 | 新开通表单+首登向导彩排,把坑在友好客户身上踩完 |
| AI 客服补多轮记忆 | 代码,~1天 | kfMessages 表已有历史,取最近 N 条传 context 即可,ROI 最高的单项修复 |
| 删 deploy-*.py、移出 worldmonitor/ | 代码,半天 | 排雷,防止新会话 AI 或人误触 |

### P1 · 30 天内:把三个假保险变成真保险(主题:规模化前提)

1. **article-skill / video-skill 接入 model-router**:主链路获得熔断+降级+重试,顺手在 chat-service 出口统一写入 cost-ledger(llm 类型),token 数据本来就有,只是没落库。
2. **LLM 成本日报**:按租户 × 任务类型聚合,进今日驾驶舱,加预算告警阈值。多租户 SaaS 没有这张表等于开店不记账。
3. **黄金样本集 eval**:挑 20-50 篇代表性选题,固化"生成→六维评分"回归脚本,每次改 prompt/换模型跑一遍,首过率跌破阈值即阻塞。这是把"老韩读全文"逐步机器化的第一步。
4. **测试基线清零 + main 分支保护**:死测试清理照交接文档推完,然后 CI 必绿才可 merge,写进红线。
5. **选题来源可配**(期刊库/选题池/热点):打通跨行业最后一公里,这是商业化方案自己点名的 P0 遗留。

### P2 · 60-90 天:还数据债 + 验证飞轮(主题:从 1 到 10)

1. Schema realignment 专项:23 张漂移表收编进 schema.ts + migrations,恢复"源码=真相"。
2. 外键级联/删租户流程设计(删除前 dry-run 报告,符合现有 DB 护栏文化)。
3. 效果回流扩展到抖音/视频号,验证选题权重真实化后首过率/采用率是否可观测提升——**飞轮要有仪表读数,不然又是信仰**。
4. 前端入口收敛(按 frontend-interaction-audit 清单,15-20 人天,可分批)。
5. 校准池独立成表 + FAQ 向量化 + RAG v1/v2 统一(article-skill 还在用旧版 retriever)。
6. 行业包架构解耦——**前提是已有 3 家以上付费客户**,否则继续押学术垂直。

---

## 五、AI 项目战略视角的五条判断

**1. 你的护城河 = 期刊数据 × 行业工作流 × 校准数据,不是模型。** 锁 DeepSeek+Qwen 是对的(成本+合规+可替换性),不要被"接更强的模型"诱惑。真正该当资产运营的是三类数据:期刊库(已有)、人工裁决校准样本(刚起步,每一次老韩采用/驳回都在给 AI 审稿员喂私有训练信号)、发布效果数据(今天刚闭环)。这三样每多积累一天,竞品追赶成本就高一天;模型能力则是所有人共享的水位。

**2. 功能开发应该刹车,收口和验证应该踩油门。** 项目的功能面已经超前商业验证 1-2 个身位。继续堆功能的边际价值趋近于零,而每一个新功能都在加重"7 个入口 5 套组件"的碎片化。建议未来 30 天冻结新功能线(除选题可配),资源全部投向 P0/P1。

**3. 把"老韩"从瓶颈变成标注员。** 当前系统里所有关键判断(质量验收、审稿裁决、选题拍板)都过老韩,这在 1 个客户时是质量保证,10 个客户时是系统崩溃点。战略路径已经在代码里了——AI 审稿员的 shadow→live 机制就是正确答案:**老韩的每次人工裁决都应该被设计成"教 AI 一次",而不是"替 AI 干一次"。** 一致率报表达标一个环节,就 live 一个环节。黄金样本集 eval 同理。目标是 90 天后老韩只看抽检和红线件。

**4. 三张报表 = AI SaaS 的经营仪表盘,比任何新功能值钱:** 成本表(每租户每月 LLM/DVH/TTS 花费 → 毛利可算)、质量表(首过率/一致率/降级率 → 卖点可证)、效果表(阅读回流/采用率/选题权重 → 续费可讲)。现在第三张刚有雏形,前两张是空白。没有这三张表,定价只能拍脑袋,续费只能靠关系。

**5. 单人+AI 模式的最大风险是 bus factor = 1,对策是"文档即组织"再进一步。** 现有红线/清单/交接文档文化很好,下一步是把验收从文档变成可执行断言(eval 脚本、CI 门禁、成本告警),让"规矩"不依赖任何人(包括任何一个 AI 会话)记得。已发生的事故(root 直连改码、false-green 部署、锁文件漏更)全都是"规矩在文档里但不在机器里"造成的。

---

## 附:证据索引(供复查)

- LLM 网关旁路:packages/server/src/services/skills/article-skill.ts:14(只 import AIProvider 类型,无 chat-service)、skills/index.ts:11
- 成本 0 记录:services/billing/cost-ledger.ts(llm 类型无调用方)、services/agents/ 内 tokensUsed: 0 共 13 处
- 假数据已拔:commit 0a36c25(2026-07-06),routes/dashboard.ts:145 注释
- 读源码型测试:__tests__ 内 readFileSync/readSrc 命中 105/198 文件
- Schema drift:docs/schema-drift-rescue-20260423.md(23 表 18 列)
- 死测试基线:交接文档-死测试清理.md(~47 失败文件)
- 客服无记忆:services/work-wechat/kf-responder.ts(全文件无 context 传参)
- 单客户证据:data/wechat-corpus/(197+460 篇均属顺仕美途两个号)、.daily-reports/2026-06-12(测试租户彩排)
- 交互碎片化:docs/frontend-interaction-audit.md
- 商业化阶段自评:docs/BossMate商业化战略优化方案.md("3-5 家付费客户验证定价"为待办)
