# CC 指令:图文线智能体补齐(决策留痕 + 可行性预检)

## 最实在的动机(8-17 现场)

追查"education 期刊每天被消耗 14 本"时,静态读代码只追到 5 本 ——
domestic 1 + international 1 + roundup 3。**剩下 9 本追不出来。**

在此之前还错了一次:先按 journal_usage 行数推出"日耗 17.9",
后来发现那个数被重试重复插入抬高了 1.4~2 倍,真实是 14。

> **静态读代码追不出产量,正说明决策留痕是刚需。**

一条内容为什么用了这本刊、这个体裁、这个模板,答案现在只存在于
"当时那次运行的控制流"里,运行完就没了。人肉考古能追到一半,
另一半只能靠猜 —— 而猜出来的数会被当成事实用下去。

## 为什么是这两件

图文线的智能体已经不少(选题/选源/生成/事实核查/格式评判/语义评判/健康闸/
合规闸/分发),缺的不是 agent,是**环**:

```
缺决策留痕  → 每次问"为什么会这样"都要人肉考古
              (这周三次:为什么全是教育、为什么同刊 17 次、为什么选题坍缩)
缺可行性预检 → 注定编造的任务照样开工,靠后面加闸兜
              (A2 治的是同一个病,但它只覆盖体裁选择这一处)
```

**两件的共同点:把"事后追查/事后拦截"变成"事前算清楚"。**

---

# 第一件:决策留痕(优先,投入产出比最高)

## 1.1 决策点清单(先盘,再改)

先 grep 出图文线上所有"从多个候选里选一个"的地方,报清单给我。已知的:

```
选题     keywords 打分排序取 TOP     ← 本周坍缩事件的现场
选刊     pickScopedFreshJournal 十层降级链
体裁     dataSupply 分级路由(A2)
模板     rotationEnabled 过滤 + 效果加权
账号     领域匹配 + 保底 + 独家分配
学科     DISCIPLINE_ROTATION / computeAutoQuota
```

## 1.2 统一的决策记录结构(唯一归宿,新文件)

```
DecisionTrace {
  point        决策点名(topic_pick / journal_pick / genre_route / ...)
  chosen       选中的是什么(id + 简短标识)
  reason       为什么选它(结构化,不是自由文本)
  candidates   看过几个候选、前几名分别是谁和为什么落选(截断到 TOP3)
  constraints  当时生效的约束(冷却/配额/池余量/学科限制)
  fallbackTier 若走了降级链,落在第几层、为什么跳过前面几层
}
```

**写进 contents.metadata.decisions[](数组,每个决策点一条)。**

⚠️ 三条硬要求:
- **不用自由文本**。"因为它最合适"这种记录等于没记 ——
  reason 必须是可枚举的原因码 + 关键数值
- **不改变任何决策行为**,只记录 —— 本件是纯观测改动,零冲击面
- **候选列表截断**(TOP3 + 总数),别把 50 本刊全塞进 metadata

## 1.3 消费方(记了要有人看,否则又是死数据)

```
① 内容详情页加一块「这篇是怎么来的」——
   运营点开能看到:选了哪本刊(为什么)、什么体裁(为什么)、
   哪个模板(为什么)。这是运营理解系统的第一扇窗
② 周报④加一行「本周决策分布」:
   选刊落在第几层的占比、体裁路由分布、被约束拦掉最多的是什么
   —— 池子枯竭这类问题会自己浮出来,不用等人去查
③ 排查时一条查询定位,不再考古
```

## 1.4 验收

```
· 拿这周三个已知事件回放:能否只靠 metadata 回答
  "为什么全是教育""为什么同一本刊 17 次""为什么选题坍缩"
  —— 答得出才算过
· 零行为变更:全量 diff 零新增,生成结果与改前一致
```

---

# 第二件:任务可行性预检(生成之前算"有没有解")

## 2.1 它和 A2 的关系

A2 解决的是"体裁选择"这一处。**预检把同一个思路提前到更上游、覆盖更全**:

```
A2      给定这本刊 → 选一个它写得出的体裁
预检    给定(刊 + 体裁 + 模板)组合 → 算需要的事实 vs 库里有的
        → 缺口过大:换体裁 / 换刊 / 直接跳过这个任务
```

## 2.2 实现

```
每个体裁声明【必需事实清单】和【可选事实清单】
  投稿指南:必需 = IF或复合IF + 审稿周期或录用率;可选 = 分区/版面费
  学科定位:必需 = 目录归属 + 学科分类;可选 = 主办方
  ...

预检 = 必需清单 ∩ 库里实际有的
  必需项缺失 → 这个组合无解 → 换体裁(A2 路由)或跳过并记 incident
  可选项缺失 → 可以写,但 prompt 里显式声明"本刊无此数据,禁止书写"
```

**复用现成件**:dataSupply 分级、fabrication-criteria 的 hasDbFact、
事实密度模块的 factsAvailable 口径 —— **不要另立一套"什么算有数据"**。

## 2.3 三条设计约束

```
① 预检结果进 decisionTrace(和第一件天然接上):
   "为什么这篇没生成"和"为什么换了体裁"从此可查
② 跳过要有上限:如果某天大批任务被判无解,说明池子或体裁配置有问题,
   落 incident + 周报报数,不能静默少产
③ 先跑影子:预检只记录不拦截,跑三天看"会拦掉多少、拦的对不对",
   数据确认后再让它真的生效 —— 冲击面先量,老规矩
```

## 2.4 验收

```
· 影子期数据:被判无解的任务占比、按体裁/学科/dataSupply 分布
· 抽 10 篇被判无解的,人工看是不是真的写不了(避免误杀)
· 正式生效后:叙述型编造的抽查命中率应下降
  (这是它的最终目的 —— 从源头掐掉注定编造的任务)
```

---

# 执行顺序

```
① 决策点清单(盘点,报我)           半天
② DecisionTrace 结构 + 三个高价值决策点先接
   (选题/选刊/体裁 —— 本周三个事件都在这三处)   1 天
③ 消费方:详情页 + 周报一行                      半天
④ 验收:三个已知事件回放                        半天
────────── 上面做完部署,是纯观测改动,风险极低 ──────────
⑤ 可行性预检:体裁事实清单 + 预检函数 + 影子模式  1-1.5 天
⑥ 影子跑三天 → 数据确认 → 正式生效
```

**⑤ 依赖 ② 已上线**(预检结果要写进 decisionTrace),别并行。

# 与在跑任务的关系

```
选题偏斜修复(分数饱和/学科配额/余量削峰)—— 优先级高于本件,先做完
发布日历      —— 本件的 ① 做完再做它,排产决策也该留痕
Phase 4       —— 不冲突,可并行
A2            —— 等老板拍板;拍了之后 A2 的路由天然进 decisionTrace
```

# 铁律

- 第一件是**纯观测**:零行为变更,全量 diff 必须零新增
- 第二件**先影子后生效**,冲击面先量
- reason 用原因码不用自由文本(可统计、可断言、不会漂移)
- metadata 写入前过合法性校验(templateId 那次的教训:没校验的字段五步之后成毒数据)
- 迁移手写、部署走 git + deploy:smart、合并前双向 diff
