# CC 待办清单

> 更新于 2026-08-02。老韩说"执行第 N 项"时，按本文件对应条目做。
> 本文件不进 git（工作文档）。做完的条目标 ✅ 并保留，便于回溯。

---

## 1. 告警体系系统性自检 🔴 优先

**为什么现在做**：这套告警是老韩几个月不管系统时的唯一眼睛，而它已知会说三种谎。
7-31 那次事故就是明证——余额告警因为 BSS 权限没开，降级成"消耗骤降才报"，
等它报的时候已经停机，老韩是靠"视频没声音"发现的。

**做法（先枚举后实测，别边查边改）**：

1. 枚举 `services/ops/daily-briefing.ts` 里所有 `items.push` 的告警项（约十几条）
2. 逐条拿生产数据实测——像查"连续零分发"那条一样
3. 产出一张表：

| 告警项 | 实测数字对不对 | 受众 | 看了能做什么 | 结论(留/改/删) |
|---|---|---|---|---|

4. **三条判据，第三条最狠**：说不出"看了能做什么"的，一律删掉别占位。
   一个天天报但没人能据此行动的告警，消耗的是整个仪表盘的信任。
5. 做完一起定改法，别边查边改

**已知要处理的**：
- 「连续 3 天零分发」= 结构性误报（三个空壳租户天天触发，唯一真跑业务的租户从没触发）。
  改法已定：**改成平台级告警**（分子分母都在平台层），不是去重
- 简报第 1 条和第 3 条重复报同一件事（Paper咨询与发表 0/2）
- 新加的期刊池告警（`journal_pool_forecast`）也要纳入自检范围
- 余额告警：BSS 权限配通后，确认那行 `BSS 拒绝: NotAuthorized` 消失、余额数字出来

---

## 2. TTS 硬失败后运营端静默（补失败回执）

**现状**：TTS 失败 → 硬中止不提交（对的，7-31 已上线）→ 但不落库、不产视频，
接口早已 202 返回。**运营点完生成，等半天什么都不出现**，只有日志和简报里有。

**最小改法**：失败也落一条 contents 记录，`status=failed`，metadata 带 reason。
理由：
- 复用现有内容列表，不用新做失败面板
- `failed` 是合法状态，自然不进可发池
- 运营在列表里能看到"这条失败了、原因是语音合成失败"，而不是石沉大海
- 与 `dvh_tts_failed` incident 对得上，查证时两边能串起来

---

## 3. OSS 桶 ACL 核查（先查，别急着改）

**发现**：`dvh-videos/` 目录**公开可读**——无签名 URL 直接 curl 200 拿到 12MB 视频。
而 CLAUDE.md 记的是"私有桶 + 签名 URL"，**记载与现状不符**。
任何人拿到 URL 就能下客户视频。

**改之前必须先查**：
```
前端播放视频用的是裸 URL 还是签名 URL？
  裸 URL  → 桶转私有会让所有视频立刻播不了（含历史的）
  签名 URL → 可以安全收紧
```

**正确方向不是整桶收紧，而是分目录**：
- `dvh-backgrounds/` 必须保持公开读（阿里云 DVH 要去拉背景图）
- `dvh-videos/` 应该私有 + 签名 URL

**先做**：查前端播放方式 + 纠正 CLAUDE.md 里"私有桶"这条错误记载。
**改 ACL 单独排一轮** —— 这类改动出错是"所有视频都播不了"级别。

---

## 4. 静音视频自动检查（把事后巡检变成生成时拦截）

已证明 `ffmpeg -af volumedetect` 能可靠识别哑巴视频（纯静音 = -91.0dB）。

**建议**：出片后自动测一次音量，静音就直接标 `failed` + 落 incident，
而不是等人来扫。钱已经花了，至少别让废片流出去。

**阈值**：常态化检查用 **-60dB**（不是 -91）。
-91 是 ffmpeg 对纯数字零的输出，一刀切很干净，
但"极轻微底噪的准静音"（如 -85dB）会漏掉。

---

## 5. 质检尺子自一致性测试（便宜但结论可能很重要）

**背景**：v4-pro 与 qwen-plus 对同一批文章评分的相关性只有 **r=0.254**，
14/30 篇差超 10 分，有的差 41 分。已判定"不切主模型"。

**但这个数字指向一个更根本的问题**：如果两个都不差的模型判断相关性这么低，
那这把尺子本身稳不稳？

**做法**：用**同一个模型、完全相同的 prompt**，对同一批 30 篇文章重评一次，算 r。

```
r > 0.8     → 模型自己稳定，只是两模型标准不同 → 现状可接受，此事到此为止
r 0.5-0.8   → 尺子有噪音但还能用，80 分线附近的文章基本靠运气
r < 0.5     → 🔴 六维评分本身就是噪音，"80 分发布线"这个概念要重新审视
              意味着现在被判 needs_review 的文章里有相当比例是误判
```

**为什么重要**：老韩走后没人校准质检标尺（交接里挂着的事之一）。
**如果尺子本身不稳，"靠人工采用/驳回来校准"这条路从一开始就走不通**
——你没法用人工反馈去校准一把每次量出不同结果的尺子。

---

## 6. 阶段1 死测试 triage（40 个红）

现在基线口径可信了（干净 worktree + symlink .env + 按 commit），可以真清一轮。

**分四类，不是三类**：

| 类 | 特征 | 处理 |
|---|---|---|
| A. 死断言 | 读的源文件已删 / 被测逻辑整个不存在 | 直接删 |
| B. 逻辑搬家 | 源文件还在，被测逻辑搬到新模块（那 10 个 prompt 守卫就是——搬进了 `journal-prompt-fields.ts`） | **不能删**，改成对新模块的行为测试 |
| C. 真回归 | 功能确实坏了 | 修代码，不是修测试 |
| D. 守卫本身该淘汰 | 守文本不守行为，且该行为已被更好的守卫覆盖 | 删，或换行为测试 |

**B 和 D 最容易判错**：B 误判成 A 就删掉了真正的保护；D 不敢删就永远留着一个天天喊狼来了的测试。

**删任何测试前问一句：它守的那个行为，现在还有谁守着？** 没人守就不能删。

顺带更新 `docs/test-baseline-triage-20260708.md`（口径已过时：203 文件/1463 测试 vs 现在 256/2291）。
**产出台账**（哪些删了/重写了/为什么），下次不用重判。

---

## 7. 杂项（顺手做）

- [ ] 清掉 `/tmp` 下 7 个旧 worktree（93MB）：`git worktree prune` + 删目录。
      注册表里留一堆 detached HEAD，下次基线对比容易选错
- [ ] 固化 `git config http.version HTTP/1.1`（HTTP/2 framing 错误已遇两次，不像偶发）
- [ ] CLAUDE.md 补三条：
  - **红线 #12 修正**：「总用例数对不上」不等于对比无效，而是**必须解释**的信号。
    能逐项解释（新增/删除测试文件的用例数恰好等于差值）→ 对比有效；解释不了 → 才是环境没对齐
  - **pm2 日志坑**：`pm2 logs --nostream` 在超大日志文件上返回的是**文件开头不是结尾**，
    904 万行时会看到几周前的陈年崩溃日志。查日志一律用 `tail`
  - **检测工具口径**：凡是"检测工具"都该和被检对象共用同一份配置出口。
    前车之鉴：`oss-check.ts` 自己用 http 而生产已 https，体检结果对不上线上等于白检

---

## 8. 国际身份判据第三处分叉 —— journal-template.ts:1194 🔴 已立项

```ts
// services/skills/journal-template.ts:1194
const inScope = !!(j.impactFactor || j.partition || (j as any).jcrFull);
```

问的是「这本刊在不在中科院国际预警名单适用范围」= **国际身份**判定。
而 `services/journals/intl-signal.ts` 就是为这个问题建的唯一真相源：

```ts
hasIntlSignal(j) = impactFactor ∥ partition ∥ casPartition ∥ casPartitionNew ∥ jcrFullHasWosEvidence(jcrFull)
```

**这处少取了 `casPartitionNew`（线上 2203 行有数据），且 `jcrFull` 只判非空而不是 `jcrFullHasWosEvidence`。**

**这是同一个坑的第三次**（`intl-signal.ts` 文件头记着前两次）：
- 7-20 `compliance/content-check.ts` —— 发现 `cas_partition` 整列为空
- 7-28 `journals/journal-kind.ts` —— 又原样挑了那三列，导致 **704 本一线国际刊被判 unknown、选刊器永远选不到、日志一句不报**

### 走法（8-06 拍板）

1. **先量冲击面**
   - `hasIntlSignal` 为真但现有条件为假的刊有多少本
   - 近 14 天内容关联到这些刊的有几篇
2. **修法唯一**：换成 `intl-signal.ts` 的 `hasIntlSignal()`，**不许出现第三种写法**
3. 后果是「多渲染正确信息」（这些刊会正确显示"✅ 不在预警名单中"板块），量完没意外直接上

### 关联

修完要从 `__tests__/journal-data-supply.test.ts` 的 `ALLOW` 白名单里**删掉这一条**。
白名单是存量基线不是豁免 —— 修一处删一处，目标清零。

---

## 9. 选刊排序加「数据完备度」权重 🟡 待评估（8-06 立项）

### 由来

8-06 实测 cn 池内部选择有偏：

| cn 池 | 总数 | 近14天被选中 | 选中率 |
|---|---|---|---|
| rich | **0** | 0 | — |
| **medium** | **447** | **3** | **0.67%** |
| **sparse** | 3219 | 176 | **5.47%** |

**更好的刊没被优先选**——sparse 选中率是 medium 的 8 倍。

不是 bug：`pickScopedFreshJournal` 的分层是 **verified → 新鲜度 → 学科对口**，
排序里**根本没有「数据完备度」这一维**，从来没考虑过。

### 收益上限已经算死：12%

cn 池 3666 本里只有 447 本能到 medium（= 有万方复合影响因子）。
**rich 在 cn 池里恒为 0**——数据源结构性缺失：LetPub 只覆盖国际刊，
万方不给审稿周期/录用率，所以国内刊在当前数据源下**永远到不了 rich**。

→ 选刊优化到极致，也只能把约一成 sparse 内容变成 medium。
→ **这条不能替代 sparse 体裁（A2）**，A2 是刚需，本条是锦上添花。

### 三条设计约束（8-06 拍板，动手前必须满足）

**① 先按学科拆 447 本的分布，对上账号的学科需求再动手。**
   medium 刊可能集中在少数学科，而账号要的学科里一本没有 ——
   那实际收益远低于 12%，甚至不值得做。**数字出来再定做不做。**

**② 权重顺序不许动学科对口的位置：对口 > 数据完备度 > 新鲜度。**
   老板抱怨过 5 次「期刊不对口」。为了多写一个复合 IF 把不对口的刊排上去，
   是捡芝麻丢西瓜。

**③ 想清楚集中化副作用。**
   447 本被优先 → 更快撞 15 天冷却去重和账号独家分配。
   **收益要按这两个约束下的实际可用量算，不能按 447 本毛数算。**

---

## 10. A2 sparse 体裁 —— 验收判据（8-07 定，写在开工之前）

三条判据，**缺一不可**（前两条早、确定性强；第三条慢但测的是最终目的）：

| # | 判据 | 测什么 | 数据来源 |
|---|---|---|---|
| ① | sparse 内容 `factsAvailable` **上升** | 学科/目录/出版社成为可写素材 | `metadata.factsAvailable` |
| ② | `citeRatio` 保持 **≥80%** | 新体裁没有变成"堆砌但不用" | `metadata.factsCiteRatio` |
| ③ | 老板标注 **good 率上升** | 写得好不好 | Golden Set 标注 |

**为什么三条都要**：①② 测的是「**有据可写**」，③ 测的是「**写得好**」。
facts 指标更早、更确定（不用等标注），但它**测不出文笔与可读性**；
good 率是最终目的，但样本慢、主观。两个都要。

### 8-07 采集的 before 基线（250 篇存量）

| 分级 | n | available 均值 | cited 均值 | 引用率 |
|---|---|---|---|---|
| rich | 60 | 4.88 | 3.37 | 69% |
| medium | 25 | 3.76 | 2.52 | 67% |
| **sparse** | **165** | **2.27** | **1.87** | **82%** |

Golden Set 基线（8-02 那轮）：**good 6% / poor 28%**。

⚠️ **匹配器已冻结** —— `discipline`/`publisher` 走字面包含，同义表述漏计是已知偏差。
A2 验收前不许改，否则 before/after 的尺子不一致（测试里有断言锁住现状）。
想修同义词匹配，等验收完一个完整周期。

### 附带记录：rich 的「有米不做」

rich 手上 4.88 条只用 3.37 条，**1.5 条闲置**——真正的"有米不做"，且发生在数据最好的内容上。
闲置最多的是 `discipline`(93 篇) 和 `publisher`(30 篇)。

**先记着不动**：60 篇样本 + 分不清「该写没写」vs「不适合写」。
等 A2 之后用同样的思路做一轮 rich 的 prompt 引导，那时样本也攒多了。

---

## 11. P1-B 车轱辘话检测 —— 🛑 已搁置（8-07 验证后停止，不写生产代码）

按预注册流程验证判别力，**三个候选指标全部淘汰**，其中两个**方向相反**。

### ① 实测数字（A=poor n=11，B=非poor n=35）

| 指标 | A组 poor | B组 非poor | 方向 |
|---|---|---|---|
| **C1** 字符 4-gram 自重复率 | 均 0.086 | 均 **0.120** | 🔴 **A 低**（反了） |
| **C2** 相邻段落 bigram Jaccard | 均 0.037 | 均 0.036 | 差 0.001（噪音） |
| **C3** 最高频 8 字短语出现次数 | 均 3 | 均 **5.7** | 🔴 **A 低**（反了） |

**为什么方向反** —— 抽查直接说明：

```
C1 最高的 3 篇：全是 rich 刊（IF 3.7 / IF9+中科院1区 / IF2.9）
C1 最低的 3 篇：全是 sparse 国内刊（北大核心+CSSCI，无 IF 无分区）
```

字符 n-gram 重复度测的是「**正文反复出现同一批数据**」——数据越多、被反复引用越多，重复度越高。
rich 内容重复度高但老板不判 poor（有料）；sparse 重复度低但老板判 poor（空洞）。

### ② ⚠️ 不要拿 C1 当「数据丰富度」的代理指标

C1/C3 实际上是 `factsAvailable` 的**影子**——而 `factsAvailable` 已经**直接测**了这件事
（见 `content-engine/fact-density.ts`，8-07 已接线上）。用影子指标只会引入噪音和误解。

### ③ 老板对「车轱辘话」的识别是稳定的

```
理由含「车轱辘话/重复」：A组 7/11  |  B组 0/35   ← 完美分离
```

**问题不在「老板说的不存在」，在「这三个指标测的不是那件事」。**
重启这条线时，他的标注可以**直接当 ground truth** 用。

### ④ 边界已确认：现有闸覆盖不了，降粒度也不行

`output-health.body_repetition` 抓的是「≥20 字整段一字不差出现 ≥3 次」，
实测命中 **A=0/11、B=1/35**，几乎不触发。与车轱辘话（换个说法说同一件事）**重叠 ≈ 0**。

**「给现有闸降粒度」这条路同样不通**——降粒度只会更多抓到 rich 内容里的合法数据复述
（正是 C1 高分那批），误伤方向和现在一样反。

### 🔓 重启条件（写死，满足才重启）

1. **A2 上线后新标一批 ≥30 篇**
2. 若「车轱辘话」理由**仍高频出现** → 说明它不是「没料硬写」的伴生症状，是独立问题
3. 那时再评估语义方案（embedding / LLM 判）—— 到那时样本够、问题也从「空洞」里剥离干净了

> 当前搁置的理由**不是「做不出来」，是投入产出比不合理**：poor 组只有 11 篇有正文，
> 做出来也验证不了；而 A2 预期直接改善 sparse 的空洞感，那 7 篇里有几篇本质是「没料硬写」。

---

## 12. 池子余量与账号需求错配 + 盘点对未排产学科盲视 🔴（8-10）

### ① 错配现状（周一 psychology+education 轮换日实测）

| | education | psychology |
|---|---|---|
| 账号需求 | **24 篇/天** | **0** |
| 池子去 15 天冷却余量 | **0 本** | **360 本**（全新，一本没用过） |
| rich 刊 | 国内池 **0 本**（结构性：数据源不给审稿周期/录用率） | **255 本**（intl） |
| 现状 | 4 本刊被用满 **30 次/月** | 完全闲置 |

**根因**：`daily-cron.ts:256` —— `const todayDisciplines = quota ? Object.keys(quota) : DISCIPLINE_ROTATION[dayOfWeek]`。
有 `quota` 时**轮换表整个不生效**，而 `computeAutoQuota` 按**账号覆盖的学科**算，账号全锁在 education
→ 周一轮换表形同虚设，psychology 一次都没排过。

### ② psychology 池子质量（给老板的论据）

```
rich   256 本 (71.1%)   ← 唯一能写"投稿指南"、事实密度最高(available 4.88)的那一层
medium  53 本 (14.7%)
sparse  51 本 (14.2%)
intl: rich 255 / medium 52 / sparse 0
```

### ③ education 疲劳度已在发生（第二个论据）

今天 25 篇用了 **17 本**刊，**17/17 全是回头刊**：

```
30 天内使用次数：2次:11本  29次:2本  30次:4本
TOP5：TESOL Quarterly 30次 / Modern Language Journal 30次 / 教育前沿 30次
      教育研究评论 30次 / System 29次
```

**30 天用 30 次 = 平均每天一次**，15 天冷却对它们已完全失效（被"放宽冷却兜底层"反复选中）。
不是"即将疲劳"，是**已经重复发同一批刊持续一个月**。

### 待老板拍板（运营决策，非技术）

**扩账号到心理学方向**，比补 education 数据便宜得多——后者要接 CNKI/万方（backlog task #104，长期未成），
前者只需运营侧调账号定位，且立刻能用上 255 本 rich 刊。

---

## 13. 期刊池盘点对未排产学科盲视 🟡 感知层 bug（8-10）

`collectPoolBriefing` 只盘点**正在被排产的学科**，psychology 完全不在它视野里
（8-10 实测：盘点条目 2 条，全是 education，提到心理学的 **0** 条）。

**「系统只看它正在用的资源」正是这次错配三天没被发现的原因。**

### 修法

`pool-inventory` 应枚举**全部学科**（至少 `DISCIPLINE_ROTATION` ∪ 账号配额学科的并集），
没被排产的学科也要在盘点里出现——哪怕只是一行「psychology：360 本可选，本周期未排产」。

这样「有池子没需求」和「有需求没池子」两类错配都能被同一份盘点看到。

---

## 14. 「学科定位」体裁：五个目录的机构名与查证入口待确认 🔴 卡老韩（8-10）

`services/journals/catalog-facts.ts` 机制已就绪但**数据全空**，五个目录（CSSCI / CSSCI 扩展版 /
北大核心 / CSCD / SCI 核心）的 `reviewed` 全是 `false`。

要确认两样：**① 编制机构全称 ② 官方查证入口 URL**。

不确认的后果是明确且可接受的：文章里「目录是什么 / 怎么查证」那一章**整章不出现**。
之所以不让模型填，是因为这是关于**真实机构**的事实断言 —— 写错一个编制机构或给错一个网址，
这个体裁「每个数字都能拿官方目录逐条数出来」的全部卖点就没了。

确认后：填字段 + 把 `reviewed` 改 `true` + 在 `reviewedBy` 留痕（如 `"老韩 2026-08-11"`）。

---

## 15. 「学科定位」若上线，飞轮需要一个体裁维度 🟡 记账（8-10）

本体裁**没有**复用 `buildHookPromptBlock` / `buildVariation` —— 逐条读过后确认它们不是通用件，
而是为「选刊投稿决策」写的数据（「数字冲击」的结构说明原文就是「甩一个硬数字
（IF/录用率/审稿天数/版面费）」，例句「录用率接近 40%，IF 还在涨」）。
本体裁禁写这些，拿它们当范例等于反向教模型编造。

代价：本体裁自己的 `GENRE_HOOKS` / `GENRE_STRUCTURES` 拿不到 `getRecipeWeights` 的效果反馈。
样例阶段无所谓；**真上线要给飞轮加体裁维度**，否则这套变体永远学不到东西。

---

## 16. 刊名归一的已知边界：6 本「(中英文)」类后缀对不上 🟢 已知边界，暂不修（8-10）

706 本 `snapshot_mismatch` 里，700 本是 SCI 核心刊（已按徽章目录处理），
真正的归一化口径差只有 **6 本**：

```
中国水土保持科学   vs  中国水土保持科学(中英文)
南水北调与水利科技 vs  南水北调与水利科技(中英文)
第二军医大学学报   vs  第二军医大学学报(改名为海军军医大学学报)
古脊椎动物学报     vs  古脊椎动物学报(中英文)
天津大学学报       vs  天津大学学报(自然科学与工程技术版)
新型炭材料         vs  新型炭材料(中英文)
```

**刻意不修**：放宽 `normName` 去括号会带来错误合并的风险（如「天津大学学报」还有社科版），
而收益是 6 本。6 本走 `snapshot_mismatch` 不出稿，比 6 本出错稿好。

---

## 17. 正文过短：23% 常态，长度闸的线等台账数据再定 🟡 已收口，待数据（8-14）

近 30 天 **214/928 篇（23.1%）正文 <800 字**。已查明**不是失败**：
`videoScript` 齐全、`condensed:false`、`qualityLoop` 正常跑完 —— 是**完整生成的短稿**。
六维评分器自己点破了：`dataDensity` 原文「全文约416字，硬数据0个」。

**当前没有流出**：已发布 0 篇，205/214 被六维分 <80 的发布线拦住。

**悬而未决的一件**：`output-health` 的 `body_too_short` 线在 **300 字**，
且只在发布/分发时跑、生成时不跑 —— 对「应当 1500 字的推荐文」形同虚设。

**为什么现在不调**：调到多少、调了会不会误伤，需要 `body_too_short` 的
命中率与真/假阳性数据支撑。**这正是 Phase 1 检查器台账的第一批产出**。
台账攒够一周后按 1.2 的判定规则看它的表现，再定线。
（现在拍一个数字 = 又一条没有依据的阈值，和 PR-Q7 那条过期注释同一个病。）

---

## 18. 「抱歉，AI暂时无法响应」落库 ✅ 已闭环（8-14 复核）

全库仅 4 条，均为 7-25/7-27 事故遗留，**全部 archived**；draft/generated 零条。
出稿健康闸 `ai_fallback_text` 词条已存在并命中（无需新增）。
8-13 修好 `finishReason`/`ok` 透传后，`chat-service` 返回 `ok:false` 时
适配器映射为 `finishReason:"error"`，下游不会再把道歉文案当正文。
**本条无遗留动作，仅存档。**

---

## 19. 模板选择三路收口（#6/#7/#8）🔴 阻塞决策留痕（8-17）

**先收口，再留痕 —— 顺序不能反。** 给一个自相矛盾的机制记账，记出来的账也是矛盾的。

图文线上「选哪个模板」有**三条各判各的路径**：

| # | 位置 | 机制 |
|---|---|---|
| #6 | `services/recommendation/daily-cron.ts:683` `pickTemplateId(weights)` | `Math.random()` 加权抽样（:689） |
| #7 | `services/skills/template-registry.ts` `pickRotatingTemplateId` | 从全部**可轮换**模板里随机 |
| #8 | `services/skills/template-preference.ts:145` `selectVariantTemplates` | 偏好变体选择 |

`daily-cron.ts:654` 的注释自己写明了：「与 `article-skill` 的 `pickRotatingTemplateId()`（从全部已注册模板挑）**各判各的**」。

**这是 8-13 轮换收口的遗留**：那次把 `rotationEnabled` 收成了唯一归宿（谁**可以**被轮换有了单一真相源），但**选择路径**仍是三条 —— 收的是候选集，没收选择器。当时守卫抓出的第三处（`routes/admin.ts` 的 `LAYOUT_POOL`）是同一个结构问题的另一个面。

**为什么阻塞留痕**：决策留痕若只接一处，另两条路径产出的内容会记出**自相矛盾的账**（同一篇内容的 metadata 说模板是 X，实际渲染用的是 Y）。而"查不出来源"正是留痕要消灭的东西。

**动作**：三路收口成一个选择器（候选集已经统一，只需统一入口），然后再接留痕。
相关：决策留痕四处（#1 选题打分 / #2 趋势分层 / #3 选刊十层 / #4 选刊三级放宽）本轮先做，模板不在其中。

---

## 附：这个项目反复出现的反模式（写代码时对照）

**降级产物必须在数据上可区分于真产物。**

已经犯了 4 次：

```
质检超时  → 给 0 分     → 下游当"内容极差"剔除
TTS 失败  → 给静音音频  → 下游当"正常音频"提交（7-31 事故）
DVH 失败  → 给占位片    → 下游当"真视频"落库并显示成功
检索失败  → 给空数组    → 下游当"无规则"全部放行
```

四次都是同一件事：**依赖失败了不报错，而是造一个"形状正确的假产物"塞给下游，下游无从分辨。**
而且每次都很难查——假产物在各种表面检查下都正常
（静音 wav 时长/格式/大小全对、占位片能播、0 分是合法数字、空数组是合法集合）。

**判断标准：如果下游拿到它时无法区分真假，那这个降级就是在制造事故。**
要么带显式标记（最好由类型强制），要么直接失败。
