# BossMate 运营 SOP · 阿里云短信接入 + 新客户开通

> 一页纸操作手册。短信只需接一次；新客户每来一个跑一条命令。

---

## 第一部分 · 阿里云短信接入（一次性，上线真客户前必做）

> 没接短信前，客户填手机号收不到验证码、登不进来。接好后永久生效。

### ① 阿里云控制台申请签名 + 模板（要审核，1–2 个工作日）

进入 **阿里云控制台 → 短信服务 → 国内消息**：

1. **申请签名**：填公司名（如「顺仕美途」），用途选「验证码」，上传营业执照。审核通过后得到 **签名名称**。
2. **申请模板**：类型「验证码」，模板内容写：
   ```
   您的验证码为 ${code}，5 分钟内有效，请勿泄露。
   ```
   审核通过后得到 **模板 CODE**（形如 `SMS_123456789`）。

> AccessKey 复用现有阿里云的（和数字人同一套，无需新建）。建议用 RAM 子账号 + `AliyunDysmsFullAccess` 权限。

### ② 服务器装 SDK（一次）

```bash
ssh bossmate-boss
cd /home/projects/bossmate/packages/server
pnpm add @alicloud/dysmsapi20170525 @alicloud/openapi-client @alicloud/tea-util
```

### ③ 配 `.env`（服务器 packages/server/.env 末尾加 3 行）

```
SMS_PROVIDER=aliyun
SMS_SIGN_NAME=你的签名名称
SMS_TEMPLATE_CODE=SMS_你的模板CODE
```

### ④ 重启 + 验证

```bash
pm2 restart bossmate
```
用自己手机在登录页选「手机号登录 → 获取验证码」，**手机真收到短信**即成功。
> 第一次真发务必盯一眼 `pm2 logs bossmate`，若 SDK 接口报错截图发我，5 分钟修好。

**接好前**：dev 模式，验证码在 `pm2 logs bossmate` 里看（仅供你自测，客户收不到）。

---

## 第二部分 · 开通一个新客户（每来一个客户跑一次）

> **7-05 更新**：开通已改为**「平台管理」页面表单操作**（白名单手机号登录后侧边栏可见，30 秒填表即开通，欢迎短信自动降级为口头通知提示）。下方 CLI 命令**保留为应急备用**（页面不可用时用）。

### ① 签约 + 收营业执照

### ② 开通（首选：平台管理页表单；备用：跑开通命令，服务器 packages/server 下）

```bash
cd /home/projects/bossmate/packages/server
pnpm provision:tenant \
  --company "客户公司全称" \
  --phone   13800138000 \
  --name    张老板 \
  --credit  91110108MA01XXXX2B \
  --legal   张某某 \
  --plan    basic
```

| 参数 | 必填 | 说明 |
|---|---|---|
| `--company` | ✅ | 公司名 |
| `--phone` | ✅ | 老板手机号（登录用） |
| `--name` | ✅ | 老板姓名 |
| `--credit` | 选填 | 统一社会信用代码（填了即标记「已认证」） |
| `--legal` | 选填 | 法人 |
| `--plan` | 选填 | `trial`/`basic`/`pro`，默认 `trial` |

成功会打印：公司、老板账号、owner id。
> 防呆：手机号或信用代码重复会拒绝，不会重复开通。

### 套餐与员工名额（`--plan` 决定）

| 套餐 `--plan` | 运营上限 | 销售上限 | 说明 |
|---|---|---|---|
| `free` | 1 | 1 | 免费档（线上存量默认） |
| `trial` | 1 | 1 | 试用，开通默认值 |
| `basic` | 2 | 2 | 标准 |
| `pro` | 5 | 5 | 高配 |

- 名额占用 = **在职成员 + 待接受邀请**；**停用一个即释放一个名额**。
- 老板在「成员管理」里看到的「运营 x/N · 销售 y/N」就是按该公司套餐算的；满了邀请按钮自动禁用。
- **客户要扩容**：改 `tenants.plan`（如 basic→pro）即生效，无需重新开通：
  ```sql
  UPDATE tenants SET plan='pro' WHERE name='客户公司全称';
  ```
- **要整体调名额数字**（比如 basic 改成 3+3）：改服务器 `packages/server/src/routes/tenant.ts` 顶部的 `PLAN_ROLE_CAP` 一处，重新部署即可，前端自动跟着显示。

### ③ 交付话术（发给客户老板）

> 您好，BossMate 已为贵公司开通。请打开 **boss-mate.cn** → 「手机号登录」→ 输入手机号 **138……** → 获取验证码即可进入。首次登录后到「设置 → 成员管理」，输入员工手机号即可邀请您的**运营 / 销售**加入。

### ④ 老板自助（无需你操作）

老板登录后：设置 → 成员管理 → 填员工手机号 + 选「运营 / 销售」→ 邀请。员工手机验证码登录即自动入职，**运营进内容工坊、销售进销售雷达**，互相看不到对方数据。

---

## 第三部分 · 发布客户端装机 + 账号登录（每台发布电脑一次）

> 客户的「内容发布」靠装在客户电脑上的发布客户端(Agent)。装好后所有号在哪台电脑登录，
> 发布就在哪台跑。**登录态按电脑存在本机，换电脑要重新扫一次码，搬不过去。**

### ① 装客户端
后台「设置 → 下载客户端」按客户系统下载客户包(Windows / Mac，免装 Node 版)，
解压后双击启动器(`start-agent.bat` / `start-agent.command`)，输入你发的 6 位配对码即可。

### ② 登录发布账号（新流程，6-22 起）

双击启动器后，程序会**自动弹出一个「添加发布账号」网页**（本机网页，不用打字）：

- 页面**上半部**列出「这台电脑还没登录的已有账号」——点账号后面的 **【登录】** → 弹出对应平台登录页 → 手机扫码，扫完即绑定本机。**已有的号一律走这里登，别用下面的新增**（新增会建一个全新号，会和已有号撞重）。
- 页面**下半部**「＋新增抖音 / ＋新增视频号」——只有要加**全新、后台还没有**的号时才点。
- 一个一个点、一个一个扫即可，可重复加多个号。

> 旧版本会一启动就自己连续弹好几个扫码窗口、拖十几分钟才出加号页——已废弃。现在**只弹这一个加号页，谁要登谁点**。客户端升级后自动生效。

### ③ 卡在「设备已吊销」怎么办
让客户双击客户包里的 **「清理旧客户端」**（Windows=`清理旧客户端.bat`，Mac=`清理旧客户端.command`）→ 你发个**新配对码** → 客户重新双击启动器、输入新码即可。
（清理工具只清这台电脑的本地身份，不动服务器数据。）

---

## 排错速查

| 现象 | 原因 / 处理 |
|---|---|
| 客户收不到验证码 | 短信没接（仍 dev 模式）→ 做第一部分；或签名/模板没审核通过 |
| 提示「获取过于频繁」 | 同号 60 秒只能发 1 条、每小时 5 条（防刷，正常），等一会 |
| 提示「尝试次数过多」 | 验证码连错 5 次锁定，重新获取 |
| 开通报「手机号已是某账号」 | 该号已开通过，换号或直接让其登录 |
| 老板登录看不到「成员管理」 | 他不是 owner/admin。开通脚本建的就是 owner，正常应能看到 |
| 员工登录满屏「需要权限」 | 已修复（6-20）；若仍有，确认服务器是最新代码 |
| 客户端启动后弹一堆扫码窗口 | 旧版本行为，升级客户端后只弹一个「添加发布账号」页；重启启动器即自更新 |
| 新电脑账号要重新扫码 | 正常：登录态按电脑存本机，换机必重扫。在「添加发布账号」页点已有账号的【登录】即可 |
| 误用「＋新增」把号建重了 | 新增=建全新号；已有号要用列表里的【登录】。重号到后台删掉即可 |

---

## 记几个固定值（接好后填这里备查）

- 短信签名：`____________`
- 短信模板 CODE：`____________`
- 平台登录地址：`https://boss-mate.cn`
- 服务器 ssh 别名：`bossmate-boss` → `/home/projects/bossmate`
