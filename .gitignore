# Dependencies
node_modules/
.pnpm-store/

# Build
dist/
build/
.next/

# Environment
.env
.env.local
# 部署目标(每台机器自己一份, 与 .env 同一套规矩) —— 见 scripts/deploy-with-fallback.sh
.deploy.local
.env.production
# task #1 root cause fix（2026-05-01）：.env 备份/影子文件证据
.env.backup*
.env.*.backup
*.backup-shadow-*

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Test
coverage/

# Runtime data
packages/server/data/

# Docker
docker-compose.override.yml
.claude/
*.~*
.~lock.*
.gstack/
.context/

# === drift rescue PR #2 补充（2026-04-25）===
# 运行时产物（顶层 data/ 是数据落盘目录；src/data/ 是源码 TS 静态模块，需 tracking）
data/
!packages/server/src/data/
# TS 增量构建
*.tsbuildinfo
# 备份文件
*.bak
*.bak.*
# macOS AppleDouble
._*
# 根目录调试脚本
/test-*.mjs
/test-*.cjs
/fix-*.js
/fix-*.cjs

# Deploy artifacts (server-side pg_dump backups, never committed)
.deploy-backups/
.review-stash/
scripts/dvh-prototype-*.ts

# PR-H: 已废弃的旧部署脚本 (被 scripts/deploy-with-fallback.sh 取代)
deploy-crawlers.py
deploy-topic.py
deploy-v2.py
.clean-main/
.review-stash/
dist-client/
dist-portable-win/
bossmate-agent-Windows-便携.zip
dist-portable-mac/
bossmate-agent-Mac-便携.zip

# 6-18: Agent 内置 Chromium 共享缓存(prepare-chromium.mjs 预下, 体积大, 不提交)
packages/agent/.chromium-cache/
packages/agent/dist-portable-*/

# 6-20: 防误提 — 爬虫产物/Python缓存/日报, 避免 git add -A 一锅端
__pycache__/
*.pyc
.daily-reports/
packages/server/scripts/raw-*
project.private.config.json

# 7-06 排雷: 防误提 — worldmonitor/ 是误入的无关第三方仓库(建议手动移出项目目录); deploy-*.py 死脚本已封存
worldmonitor/
deploy-*.py

# 7-22 SVG 图表快照 = svg-charts.test.ts 每次跑 writeFileSync 生成的可视化样品(非断言基线)
# 跟踪它 → 任何环境跑一次测试就产生 diff → 卡 deploy 的 git 操作(已 abort 两次)。改为不跟踪, 文件仍生成可看。
packages/server/src/__tests__/fixtures/snapshots/*.svg

# === 治理建议（2026-08-28）===
# 内部办公文档(docx/pptx/xlsx)建议不入库, 改用网盘/内部知识库分发;
# 历史遗留文件已移至 docs/internal/, 新增的一律忽略:
docs/internal/
