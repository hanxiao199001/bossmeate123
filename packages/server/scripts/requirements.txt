# ⚠️ 2026-07-25 勿按本文件在生产装 scrapling —— 装上会产生假数据，比不装更糟。
#
# 实测(生产机)：生产机本来就**没装** scrapling，journal_scraper.py 一直返回
# {"error":"Scrapling 未安装"}，所有 LetPub 富化路径静默空转(无数据但无害)。这次按本文件装上
# scrapling 0.4.11 + [fetchers] extra 后，脚本能跑了，但**解析全错**：任何刊都返回
#   impactFactor = 2026.0（抓到的是年份）、name = '按研究方向查看：'（抓到的是导航文案）
# 即 journal_scraper.py 的选择器是 0.2 时代写的，对 0.4.x + 现网 LetPub 页面结构已失效。
# Springer 那半边(publisher / website)仍正确，坏的是 LetPub 半边。
#
# 危险在于 7-25 新上的 persistTrustedJournalFacts 会把它当"可信源"回写 journals 表，且规则是
# "只填空不覆盖"→ 假 IF 会永久钉死；4394 本 IF 为空的刊会在每日 03:00 cron 里被批量污染，
# 生成的文章标题会直接引用假 IF（实测产出过「2026逆天影响因子」的稿子）。
#
# 重新启用前必须先做两件事：
#   ① 按 0.4.x API + 现网页面重写 journal_scraper.py 的 LetPub 选择器，并加解析自检
#      （IF 落在 0<x<100、抓到的刊名与查询名相似）；
#   ② 在 persistTrustedJournalFacts 里加同样的取值合理性闸，脏值宁可不写。
# 依赖本身：0.4 把 fetcher 依赖拆进 extra，要装 "scrapling[fetchers]"（会带 playwright/
# patchright/browserforge）；系统 python3 无 pip 时需先 apt install python3-pip。

scrapling>=0.2
curl_cffi>=0.7
