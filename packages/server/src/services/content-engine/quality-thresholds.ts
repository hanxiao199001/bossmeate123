/**
 * 质量分数线 —— **全系统唯一真相源**(纯常量, 零 IO 零依赖)。
 *
 * ## 病根
 * 架构审计查出全系统有 6 套分数线散落各处, 都是裸字面量:
 *   quality-check-v2 的 80/6, quality-check-engine 的 70, crawl-data-sink 的 70,
 *   article-skill 的 70, quality-checker 的 85 + env.QUALITY_MIN_SCORE, content-worker 的动态阈值。
 * 光看代码根本分不清「这个 70 和那个 70 是不是一回事」—— 事实上不是:
 * 有的是六维总分、有的是四维总分、有的是知识库沉淀门槛、有的是发布门槛。
 * 数字撞在一起纯属巧合, 而巧合会让人误以为改一个就改全了。
 *
 * ## 本表的规矩
 * 1. 每条必须写清 **语义 / 适用环节 / 为什么是这个数**。只有数字没有语义的条目不许进。
 * 2. 消费方一律 import, 不许再写字面量。
 * 3. 数值本身**不是**随便改的 —— 它决定什么内容能发出去。改前先看"为什么是这个数"。
 *
 * ⚠️ 收口进度: 本表目前收了六维体系 + Agent 自动放行线。
 *    仍待收口(散在各自文件里, 见架构修复设计框架 阶段1 清单):
 *      - `data-collection/quality-check-engine.ts` 的四维总分 70 与评级 80/70
 *      - `data-collection/crawl-data-sink.ts` 的 MIN_QUALITY_SCORE = 70
 *      - `skills/article-skill.ts:391` 的 >= 70
 *      - `content-engine/content-worker.ts` 的动态 threshold
 *    这几处语义与六维体系不同(采集质检 / 知识库沉淀 / 生成自检), 收口时必须分别命名, 别并成一个数。
 */

// ─────────────────────────────────────────────────────────────
// 六维质检体系(生成主线, quality-check-v2)
// ─────────────────────────────────────────────────────────────

/**
 * 六维总分发布线。
 * - 语义: 六个维度加权总分, 达到即"可进可发池"(仍需过编造/合规/出稿健康等硬闸)。
 * - 环节: `sixDimQualityCheck` 判 passed; 反馈文案里的"X 分发布线"也用它。
 * - 为什么是 80: 老韩人工采用/驳回校准出的线, 详见 `内容质量评分标准-80分线.md`。
 *   低于此线的稿子人工复核时驳回率显著上升, 是经验值不是拍脑袋。
 */
/**
 * 🔴 六维评分标尺版本号。**改评分 prompt 必须同时 +1**（老韩 8-21 立）。
 *
 * ## 为什么必须有
 *
 * 8-21 决定给六维判据动刀（解除双重计分 / practicality 体裁中立化 /
 * topicHook 去硬上限 / structureDensity 补中间档）。改完之后：
 *
 * ```
 * quality_verdict 已打的标   = 旧尺子
 * 周报的 15% 达标率           = 旧尺子
 * 8-20 那张 328 篇的维度表    = 旧尺子
 * 明天起的所有分              = 新尺子
 * ```
 *
 * 不带版本号，一周后没人分得清哪个分是哪把尺子打的 ——
 * **这是红线 #20 最经典的形态，而且这次是我们主动制造的。**
 *
 * ## 规矩
 *
 * 1. 任何一次评分 prompt 文字改动（判据措辞、档位、权重、加减维度）→ 版本 +1。
 *    prompt 之外的改动（重试策略、超时、模型路由）**不**动版本 —— 那不改变尺子。
 * 2. 落库：`metadata.sixDimScoringVersion` + `content_publish_log.scoring_version`。
 * 3. **跨版本趋势对比一律禁止**，除非显式标注两侧版本。
 *    「本周达标率比上周高」在跨版本时是无意义的句子。
 * 4. 存量无此字段的行 = v2 之前，按 `SCORING_VERSION_LEGACY` 处理。
 *
 * ## 版本史
 *
 * - `v2` —— 8-21 之前的既有尺子（历史上没打过版本号，回填时按 legacy 处理，
 *           但语义上就是它）。已知偏见：judged 的是「像不像一篇投稿指南」——
 *           practicality 判据只认投稿建议、topicHook 痛点绑投稿且惩罚陈述句开场、
 *           structureDensity 与 dataAccuracy 双重计分（合计权重 45%）。
 * - `v3` —— 8-21 起。逐件放开，见各维判据处的注释。
 */
export const SIX_DIM_SCORING_VERSION = "v2";

/** 存量（8-21 之前落库、没有版本字段）的占位。**不要**把它当成 v2 的同义词用于统计口径。 */
export const SCORING_VERSION_LEGACY = "legacy";

export const SIX_DIM_PUBLISH_TOTAL = 80;

/**
 * 六维单维最低分。
 * - 语义: 任一维度低于此值即不通过, 即使总分够 —— 防"五项优秀掩盖一项致命"。
 * - 环节: 与 SIX_DIM_PUBLISH_TOTAL 一起构成 passed 判据(总分够 **且** 每维够)。
 * - 为什么是 6: 满分 10 分制下 6 分是"勉强及格", 再低说明该维度实质缺失(如全文零数据支撑)。
 */
export const SIX_DIM_PUBLISH_MIN_DIM = 6;

/**
 * 六维"优秀"线(仅用于反馈文案措辞, 不参与放行判定)。
 * - 语义: 给运营看的评价词, 高于此分显示"内容质量优秀"。
 * - 环节: `summarizeSixDim` 之类的文案生成。
 * - 为什么是 90: 纯展示分档, 与发布无关; 改它只影响措辞不影响什么能发。
 */
export const SIX_DIM_EXCELLENT_SCORE = 90;

/**
 * 低分维度提示线(反馈文案里"低分维度"列出哪些)。
 * - 语义: 低于此分的维度会被逐个列出并给修改建议。
 * - 为什么是 8: 比及格线 6 高, 因为这是"值得再抬一抬"的提醒而非"不合格"判定。
 */
export const SIX_DIM_WEAK_DIM_HINT = 8;

// ─────────────────────────────────────────────────────────────
// Agent 质检(services/agents/quality-checker.ts)
// ─────────────────────────────────────────────────────────────

/**
 * Agent 自动放行线。
 * - 语义: 六维总分达到此值 **且** 其余检查全过 → 自动 approved, 不经人工。
 * - 环节: QualityChecker Agent 的 decide()。
 * - 为什么是 85(高于发布线 80): 自动放行等于放弃人工兜底, 门槛必须严于"人工复核后可发"。
 * - ⚠️ 未评上分(totalScore === null)时**绝不自动放行** —— 未评分不是高分也不是低分, 一律转人工。
 *   这是 7-27 事故("没评上分"被当成"评了 0 分")在裁决侧的同一条教训。
 */
export const AGENT_AUTO_APPROVE_SCORE = 85;
