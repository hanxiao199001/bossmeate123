# 交接文档 · 死测试清理（Task #1）

> 批1(pr116/pr123)已做完验证方法，本文档供 fresh 会话照模板推完剩余。前置 Task #2(storage懒加载)/#3(付费防丢)已完成。

## 背景 · 当前基线

`cd packages/server && pnpm vitest run` 现状（截至 a40d5d7）：约 **47 失败文件 / ~86 失败测试**（storage崩已修、付费3连败已修、批1两文件已绿）。这些红是全仓回归读数的噪声源，清完后基线该只剩明确标注的已知失败。

## 三分法（每个失败文件逐断言判）

1. **确死** — 断言目标(组件/页面/字面)已删除且无存活取代 → **删断言/describe块**。crash-load 型(readFileSync 已删文件在 describe body)优先，一删解锁整套。
2. **死活混装** — 同文件里死断言 + 活断言并存 → **摘死留活**（批1 pr116/pr123 就是这类，见下 diff）。
3. **缓刑** — 拿不准是"断言目标演进了(该更新断言)"还是"真行为回归(该修生产)" → **列入缓刑名单给老韩过目，别硬删/硬改**。
   - 判据：源码里**目标概念仍在但写法变了** → 演进，更新断言到现状(附留痕)；**目标彻底消失且无取代** → 确死；**无法确定** → 缓刑。

## 留痕格式（每个改动 commit 必写）

`断言目标 X 已于 <commit> 删除/被 Z 取代` 或 `行为已于 <commit> 演进为 Y，测试跟进`。
查证命令：`git log --oneline --diff-filter=D -- <被删文件路径>`（找删除 commit）。

## 已删组件 → 取代者（留痕用，已查证）

7 个前端组件删于 **`43668dd`**「首页合并进今日驾驶舱 + 下线/chat整页 + 清9个死页面 + 修/content死链」：
`HeroSection / Pipeline24hStrip / PreviewCardRow / DashboardPage`（首页→今日驾驶舱 TodayPage）、`ContentPage`（内容管理→ContentWorkbenchPage/ContentDetailPage）、`ChatPage`（/chat 整页下线）、`JournalsAdminPage`（→JournalDetailPage 等）。

## 确死来源名单（读上述已删组件的 10 个测试文件）

`p0-hero-dashboard` · `p0-sidebar-layout` · `p1-workbench` · `pr116-p3-frontend`✅ · `pr119-p4-frontend` · `pr123-p6-preferences`✅ · `pr141-content-type-video` · `pr178-content-management` · `pr192-manual-cover-upload` · `q7-dashboard-widget-and-modal`
（✅=批1已做。其余 8 个多为死活混装，逐个摘。）

## 批1 diff 模板（照抄手法）

- **确死删块**：pr116 删整个「ContentPage 入口」describe（读已删 ContentPage.tsx），保留同文件「RecommendationModal」describe（组件仍在 → 活）。
- **死活拆 + 清无用 helper**：pr123 删「boot+前端」describe 里 3 条 ChatPage 断言 + `readWeb` 定义（唯一调用方随 ChatPage 移除），保留 index.ts/schema/service/route 后端断言。
- **演进断言更新**：pr123 `article-skill fallback链` 的 `?? getDefaultTemplateId` 已演进为 `?? (rotation ? pickRotatingTemplateId() : getDefaultTemplateId())`(PR-G) → 断言改为匹配现状 + 留痕注释。

## 推进纪律

- 分批(每批2-4文件一个 commit)，不一口气全删防误伤。
- 每批做完 `pnpm vitest run <这批文件>` 确认操作过的**活断言真绿**。
- **缓刑名单**：拿不准的**不动**，攒一份清单（文件+断言+疑点）交老韩，他拍了再动。
- 另有一类**非死测试的失败**（真行为断言 toHaveBeenCalled/toBe 等，非 readSrc 源码断言）→ 不属本任务，单独记，可能是真 bug 或 mock 不全（参考 Task #3 付费测试的排查法：对 git log 看逻辑演进）。

## 获取当前失败清单

```
cd packages/server && pnpm vitest run 2>&1 | sed 's/\x1b\[[0-9;]*m//g' > /tmp/run.txt
grep -B2 -iE "ENOENT|no such file" /tmp/run.txt   # 确死(读已删文件)优先
grep -iE "to match|readSrc|readFileSync" /tmp/run.txt  # 源码断言(疑死/疑演进)
```

## 终点定义（老韩定）

两任务(本 #1 + 已完成的 env基建)清完后，全量 vitest 应**全绿或只剩明确标注的已知失败**，然后 CLAUDE.md 加一条：**「全量测试基线已清零，新增失败=阻塞项，不得带失败合并」**。此后测试读数才是护栏不是噪音。
