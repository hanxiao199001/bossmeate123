# BossMate 服务器迁移手册（旧机 122.152.234.155 → 新机 119.91.52.13）

> 目标：把整套代码 + 数据库 + 存储文件全量迁到新轻量机（Ubuntu 2核4G，广州），接上备案好的域名 **boss-mate.cn**（HTTPS），旧机保留作回退。
>
> 约定：下面命令里 **`旧机` = 122.152.234.155**，**`新机` = 119.91.52.13**。`<DB密码>` 指旧机 `.env` 里 `DATABASE_URL` 中的密码（务必一致）。
>
> ⚠️ 三条铁律先记住：
> 1. **`.env` 必须从旧机原样拷过来**——尤其 `CREDENTIALS_KEY`（加密了所有账号凭证）、`JWT_SECRET`（改了所有人要重登）。这俩变了 = 旧账号凭证全部解不开、登录态全失效。
> 2. **数据库密码新机要和旧机 `.env` 里一致**（因为 `.env` 整份拷过来，里面写的就是旧密码）。
> 3. 迁移期间**旧机别停**，新机验证通过再切域名。

---

## 阶段 0 · 先在旧机摸清家底（在旧机执行）

```bash
ssh ubuntu@122.152.234.155
cd /home/projects/bossmate

# 0.1 找到 .env 实际位置并看关键变量(别外泄)
ls -la .env packages/server/.env 2>/dev/null
grep -E "DATABASE_URL|REDIS_URL|CREDENTIALS_KEY|JWT_SECRET|UPLOAD_DIR|LANCEDB_PATH" packages/server/.env 2>/dev/null || \
grep -E "DATABASE_URL|REDIS_URL|CREDENTIALS_KEY|JWT_SECRET|UPLOAD_DIR|LANCEDB_PATH" .env

# 0.2 确认数据目录大小(视频可能很大, 估算 rsync 时间)
du -sh packages/server/data 2>/dev/null
du -sh packages/server/data/uploads/storage 2>/dev/null   # DVH 视频在这

# 0.3 确认 PG 库名/版本
sudo -u postgres psql -c "\l" | grep -i bossmate
psql --version
```

记下：`.env` 路径、`<DB密码>`、`data` 目录大小、PG 库名（应为 `bossmate`）。

---

## 阶段 1 · 新机装环境（在新机执行，全新 Ubuntu）

```bash
ssh ubuntu@119.91.52.13   # 或用腾讯云控制台「执行命令」

# 1.1 基础更新 + 工具
sudo apt update && sudo apt upgrade -y
sudo apt install -y curl git rsync build-essential ffmpeg fonts-noto-cjk ufw

# 1.2 Node 22 (与项目 engines >=22 一致)
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt install -y nodejs
node -v    # 应 v22.x

# 1.3 pnpm 9.15 (项目 packageManager 锁定) + pm2
sudo npm i -g pnpm@9.15.0 pm2
pnpm -v && pm2 -v

# 1.4 PostgreSQL + Redis + nginx + certbot
sudo apt install -y postgresql postgresql-contrib redis-server nginx certbot python3-certbot-nginx
sudo systemctl enable --now postgresql redis-server nginx

# 1.5 PM2 开机自启(按提示把它打印的 sudo 命令再执行一遍)
pm2 startup
```

---

## 阶段 2 · 新机建数据库角色和库（在新机执行）

> 密码用旧机 `.env` 里的 `<DB密码>`，保证拷过来的 `.env` 能直接连上。

```bash
sudo -u postgres psql <<'SQL'
CREATE ROLE bossmate WITH LOGIN PASSWORD '<DB密码>';
CREATE DATABASE bossmate OWNER bossmate;
GRANT ALL PRIVILEGES ON DATABASE bossmate TO bossmate;
SQL
```

---

## 阶段 3 · 迁代码 + .env（在新机执行）

```bash
sudo mkdir -p /home/projects && sudo chown -R $USER:$USER /home/projects
cd /home/projects
git clone https://github.com/hanxiao199001/bossmeate123.git bossmate
cd bossmate

# 3.1 从旧机拷 .env(关键!) — 在新机上拉
#     旧机 .env 若在 packages/server/.env 就改成对应路径
scp ubuntu@122.152.234.155:/home/projects/bossmate/packages/server/.env  packages/server/.env
#     (若旧机 .env 在仓库根, 用: scp ...:/home/projects/bossmate/.env  ./.env )

# 3.2 改 .env 里两处(只改这两处, 其余原样):
#     - ALLOWED_ORIGINS 加上新域名
#     - DATABASE_URL / REDIS_URL 保持 localhost(新机本地的 PG/Redis, 不要指向旧机)
nano packages/server/.env
```

`.env` 里把 `ALLOWED_ORIGINS` 改成（保留原有的，追加新域名）：

```
ALLOWED_ORIGINS=https://boss-mate.cn,https://www.boss-mate.cn,http://119.91.52.13,http://localhost:5173
```

确认 `DATABASE_URL=postgresql://bossmate:<DB密码>@localhost:5432/bossmate`、`REDIS_URL=redis://localhost:6379` 指向**本机**。

---

## 阶段 4 · 迁数据库（旧机 dump → 新机 restore）

```bash
# 4.1 旧机导出(在旧机执行)
ssh ubuntu@122.152.234.155
cd /home/projects/bossmate
pg_dump "postgresql://bossmate:<DB密码>@localhost:5432/bossmate" -Fc -f /tmp/bossmate.dump
ls -lh /tmp/bossmate.dump
exit

# 4.2 拷到新机并恢复(在新机执行)
scp ubuntu@122.152.234.155:/tmp/bossmate.dump /tmp/bossmate.dump
pg_restore --no-owner --role=bossmate -d "postgresql://bossmate:<DB密码>@localhost:5432/bossmate" /tmp/bossmate.dump
# 看到少量 "already exists" 警告正常(扩展等), 报错 0 即可
```

---

## 阶段 5 · 迁存储文件（视频/上传/向量库，在新机执行）

```bash
cd /home/projects/bossmate
# data 里有: uploads/storage(DVH视频)、lancedb(知识库向量)。rsync 增量, 可重复跑。
rsync -avz --progress \
  ubuntu@122.152.234.155:/home/projects/bossmate/packages/server/data/ \
  packages/server/data/
# logs 可迁可不迁(不影响功能):
# rsync -avz ubuntu@122.152.234.155:/home/projects/bossmate/packages/server/logs/ packages/server/logs/
```

---

## 阶段 6 · 装依赖 + 构建 + 迁移 + 启动（在新机执行）

```bash
cd /home/projects/bossmate
PUPPETEER_SKIP_DOWNLOAD=1 pnpm install --frozen-lockfile
pnpm --filter @bossmate/server build
pnpm --filter @bossmate/web build
pnpm --filter @bossmate/agent build || echo "agent build 失败不阻断(一键客户包暂不可用)"
pnpm --filter @bossmate/server db:migrate    # 幂等; dump 已含表则多为 no-op

# 启动服务(首次用 start, 之后 deploy 脚本用 restart)
cd packages/server
pm2 start dist/index.js --name bossmate-server --update-env
cd /home/projects/bossmate
pm2 save

# 自测后端(本机)
curl -s http://localhost:3000/api/v1/health || curl -s http://localhost:3000/api/v1
pm2 logs bossmate-server --lines 30   # 看有无报错(凭证解密失败=CREDENTIALS_KEY 不对)
```

---

## 阶段 7 · nginx + 域名 + HTTPS（在新机执行）

```bash
sudo tee /etc/nginx/sites-available/bossmate >/dev/null <<'NGINX'
server {
    listen 80;
    server_name boss-mate.cn www.boss-mate.cn;

    # 前端静态(构建产物)
    root /home/projects/bossmate/apps/web/dist;
    index index.html;

    client_max_body_size 60m;

    # API
    location /api/ {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
    }
    # Agent 接口 + 静态视频(都在后端)
    location /agent/      { proxy_pass http://127.0.0.1:3000; proxy_set_header Host $host; proxy_read_timeout 600s; }
    location /agent-admin/{ proxy_pass http://127.0.0.1:3000; proxy_set_header Host $host; }
    location /storage/    { proxy_pass http://127.0.0.1:3000; proxy_set_header Host $host; proxy_read_timeout 600s; }
    location /wechat/     { proxy_pass http://127.0.0.1:3000; proxy_set_header Host $host; }
    location /work-wechat/{ proxy_pass http://127.0.0.1:3000; proxy_set_header Host $host; }

    # 前端路由(SPA): 其余都回 index.html
    location / { try_files $uri $uri/ /index.html; }
}
NGINX

sudo ln -sf /etc/nginx/sites-available/bossmate /etc/nginx/sites-enabled/bossmate
sudo rm -f /etc/nginx/sites-enabled/default
sudo nginx -t && sudo systemctl reload nginx

# HTTPS(域名已解析到本机才能签发)
sudo certbot --nginx -d boss-mate.cn -d www.boss-mate.cn --non-interactive --agree-tos -m 你的邮箱@example.com --redirect
sudo certbot renew --dry-run   # 验证自动续签
```

---

## 阶段 8 · 防火墙/安全组（新机）

腾讯云控制台 → 这台轻量机 → **防火墙**，放通：**22(SSH)、80(HTTP)、443(HTTPS)**。
**不要**对外放通 3000（后端只走 nginx 反代）。

```bash
# 系统层 ufw(可选, 与云防火墙双保险)
sudo ufw allow 22,80,443/tcp && sudo ufw --force enable
```

---

## 阶段 9 · 验证（浏览器 + curl）

- `https://boss-mate.cn` 能打开、能登录、今日页有数据（数据迁过来了）。
- 内容工坊能看到历史内容、账号矩阵能看到历史账号（凭证未报解密失败）。
- `https://boss-mate.cn/storage/...` 某条视频能播放（存储迁过来了）。
- `pm2 logs bossmate-server` 无 `CREDENTIALS_KEY`/解密 报错。

---

## 阶段 10 · 切换 + 收尾

1. **DNS**：boss-mate.cn 已解析到 119.91.52.13（备案已过）。确认 A 记录无误。
2. **改部署默认指向新机**（本地仓库，改一行后提交）：
   `scripts/deploy-with-fallback.sh` 里
   `SERVER="${BOSSMATE_DEPLOY_SERVER:-ubuntu@122.152.234.155}"`
   → 改成 `ubuntu@119.91.52.13`。以后 `pnpm deploy:smart` 默认就发新机。
3. **Agent 客户端**：存量客户的便携包里 `bossmate.cfg` 的 `SERVER_URL` 还指向旧机 IP。
   - 旧机保留几天作回退，存量客户暂不受影响。
   - 之后让客户**重新下载新便携包**（网页一键下载会写入新的 `SERVER_URL=https://boss-mate.cn`），重新配对即可。
   - ⚠️ 确认网页"下载客户包"生成的 `SERVER_URL` 已是新域名（在账号矩阵/设置页下载时核对；如仍是旧 IP，需在生成配置处改成 `https://boss-mate.cn`）。
4. **观察 2-3 天**新机无异常后，再停旧机（`pm2 stop bossmate-server`）。

---

## 回退方案（出问题随时退回旧机）

- DNS 把 boss-mate.cn 改回旧机，或直接用旧机 IP。
- 旧机一直没停、数据是迁移那刻的快照——迁移期间若新机已产生新数据，回退会丢这部分，所以**切换后尽快确认**，别拖太久双写。

---

## 速查：要拷的关键东西
| 内容 | 来源 | 去向 |
|---|---|---|
| 代码 | git clone | /home/projects/bossmate |
| `.env`(含密钥) | 旧机 packages/server/.env | 新机同路径 |
| 数据库 | pg_dump bossmate | pg_restore bossmate |
| 存储(视频/上传/向量) | 旧机 packages/server/data/ | 新机同路径 |
