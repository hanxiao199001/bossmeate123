# 企微 AI 客服开通 — 管理员操作单（10 分钟）

> 目的：给公司企微接上 AI 智能客服（自动接待微信咨询、答期刊问题，复杂的转人工并通知运营）。
> 需要管理员在电脑上操作：浏览器打开 **work.weixin.qq.com** → 用管理员企微扫码登录管理后台。

## 方式一（推荐，2 分钟）：授权给韩肖，剩下他自己弄

1. 管理后台 → **我的企业** → **权限管理** → **分级管理员** → 添加
2. 选择成员：**韩肖**
3. 勾选权限：**应用管理**、**微信客服**（或"客户联系"）、**通讯录（仅查看）**
4. 保存。完事。

## 方式二：管理员自己配（10 分钟，照下面点）

**① 开通微信客服**
应用管理 → 找到「微信客服」→ 开通 → 添加一个客服账号（名字如"顺仕美途学术咨询"）

**② 拿两个 Secret**（企微会把 Secret 发到管理员自己的企微消息里，转发给韩肖即可）
- 微信客服 Secret：「微信客服」应用页 → API/开发配置 → 查看 Secret
- 自建应用 Secret：应用管理 → 自建应用（现有那个）→ 详情页 → 查看 Secret

**③ 配回调**（在微信客服的"回调配置"里）
- URL：`https://【问韩肖】/api/v1/work-wechat/callback`
- Token、EncodingAESKey：点"随机生成"，生成后把这两串发给韩肖
- （先别点"保存验证"，等韩肖把 Token 写入系统后再验，一次就绿）

**④ 企业可信 IP**
自建应用详情页 → 企业可信 IP → 添加：`【服务器出口 IP，问韩肖】`

**⑤ 顺手看一眼认证状态**
我的企业 → 企业信息 → 若显示"未验证"，告诉韩肖（可用公司认证公众号授权，几分钟，不用法人跑流程）

## 最后交给韩肖的东西（方式二才需要）

企业 ID（我的企业页最下方 corpId）、微信客服 Secret、自建应用 Secret、回调 Token、EncodingAESKey——五样发他企微即可。

> 安全说明：Secret 只存在公司自己的服务器上（加密存储），AI 客服答不上来的自动转人工，不会乱承诺；每条对话都有记录可查。

---
## ⚠️ 测试/演示铁律（7-07 联调血泪）
- **必须用一个从未进过该客服的全新微信号**当"客户"扫码进。旧号（进过一次的）会卡在接管前旧状态，新消息不触发回调 → AI 静默不回，**给客户演示时踩到极难看**。
- 回调 URL 用 **HTTPS 域名** `https://boss-mate.cn/api/v1/work-wechat/callback`（IP+http 会被 nginx 404）。
- 静默不回排障：先 grep nginx access.log 看有没有 **POST**（无=消息没到服务器，多半旧会话号/发错入口）→ 有 POST 再查 app 日志 sync_msg/意图分类/send_msg。
